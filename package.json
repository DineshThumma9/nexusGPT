{
  "name": "centralgpt-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root configuration for CentralGPT workspace",
  "scripts": {
    "format": "npm --prefix centralGPT-frontend run format && (cd centralGPT-backend && uv run ruff format . && uv run ruff check --select I --fix .)"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.19"
  }
}
